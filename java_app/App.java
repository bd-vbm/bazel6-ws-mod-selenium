
import org.openqa.selenium.remote.RemoteWebDriver;

public class App {
    public static void main(String[] args) {
        System.out.println("Selenium on classpath, driver type = " + RemoteWebDriver.class.getName());
    }
}
